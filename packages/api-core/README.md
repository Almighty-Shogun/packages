# API Core

A small package for core utilities for building type-safe API clients with Axios, including Laravel localization messages.

## 📃 Prerequisites
- **[Node.js](https://nodejs.org/en/)**: >= v23.11.1

## 📦 Building
- Use the command `bun install` in the root directory to install the required dependencies.
- Run `bun --cwd packages/api-core build` to build the package.
